# Test profile overrides — used by @SpringBootTest slices
spring.datasource.url=${DATASOURCE_URL:jdbc:postgresql://localhost:5432/financial_auditor_test}
spring.datasource.username=${DATASOURCE_USERNAME:postgres}
spring.datasource.password=${DATASOURCE_PASSWORD:postgres}

spring.ai.openai.api-key=${OPENAI_API_KEY:sk-test-not-configured}
langchain4j.openai.api-key=${OPENAI_API_KEY:sk-test-not-configured}

spring.flyway.enabled=true
spring.threads.virtual.enabled=true
